# 1537paperstreet — чеклист работ

Задачи для AI-агентов. Читать вместе с `PLAN.md` и `AGENTS.md`.

**Правила работы с этим файлом**

- Одна задача = один PR = одна ветка `task/T-xxx-short-name`.
- Задача считается закрытой, только когда выполнены **все** пункты Definition of Done (см. `AGENTS.md`) и критерии приёмки самой задачи.
- Зависимости указаны в скобках: `(← T-012)` значит «нельзя начинать, пока не закрыта T-012».
- **ID задач стабильны и не связаны с порядком фаз.** Фаза P9 (редактор) была добавлена позже, поэтому её номера T-160+ идут после T-105+. Ориентироваться на порядок фаз, а не на числа в ID.
- Не менять формулировку задачи. Если задача сформулирована неверно — открыть issue и остановиться.
- Не выходить за рамки задачи. Рефакторинг «заодно» — отдельная задача.

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано

---

## P0 — Бутстрап и инфраструктура

- [x] **T-001** Инициализировать Cargo workspace: `ps-core`, `ps-render`, `ps-app`. `rust-toolchain.toml` с пином стабильной версии.
  - Приёмка: `cargo build --workspace` проходит; в каждом крейте `#![warn(missing_docs)]`.
- [x] **T-002** Настроить Tauri v2 + Vite + Svelte 5 + TypeScript в `ui/`.
  - Приёмка: `cargo tauri dev` открывает окно; `npm run build` собирает бандл.
- [x] **T-003** Профиль release: `lto="fat"`, `codegen-units=1`, `panic="abort"`, `strip="symbols"`, `opt-level=3`.
  - Приёмка: `cargo build --release` проходит, размер бинарника записан в `benches/baseline.json`.
- [x] **T-004** Линтеры и форматтеры: `rustfmt.toml`, `clippy.toml` (`-D warnings`), ESLint + Prettier, `.editorconfig`.
- [x] **T-005** CI (GitHub Actions, раннер `macos-14`): джобы `fmt`, `clippy`, `test`, `build`. Кеш cargo и npm.
  - Приёмка: PR с ошибкой clippy падает в CI.
  - Проверено в PR [#1](https://github.com/mpakus/downey.jr/pull/1): намеренный `needless_bool` завершил Clippy job с exit code 101; PR закрыт без merge.
- [x] **T-006** `cargo-llvm-cov` + `vitest --coverage`, публикация отчёта как артефакта CI. Порог пока не блокирующий.
- [x] **T-007** Каркас документации: `README.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `docs/adr/000-template.md`, mdBook-скелет в `docs/`.
- [!] **T-008** ⚠️ **Ранняя проверка подписи и нотаризации** на пустом приложении. Не откладывать на P13 — это главный риск проекта.
  - Приёмка: подписанный и нотаризованный `.app` запускается на чистой машине без предупреждений Gatekeeper.
  - Решение владельца от 2026-08-13: временно пропустить Apple-сертификаты и публиковать unsigned DMG на сайте. Gatekeeper отклоняет текущий артефакт как `no usable signature`; критерий приёмки T-008 не выполнен.
- [x] **T-009** `cargo-deny` + `cargo-audit` + `npm audit` в CI. Зафиксировать список разрешённых лицензий.

---

## P1 — `ps-core`: конфиг и проекты

- [x] **T-010** `error.rs`: единый `Error` через `thiserror`, без `unwrap`/`expect` в библиотечном коде.
- [x] **T-011** `paths.rs`: разрешение `~/.1537paperstreet/`, создание дерева каталогов (`themes/`, `cache/`, `logs/`), переопределение корня через env для тестов.
- [x] **T-012** `store.rs`: атомарная запись JSON (temp → fsync → rename), дебаунс 500 ms, flush при закрытии. (← T-011)
  - Приёмка: тест «убить процесс во время записи» не оставляет битого файла.
- [x] **T-013** `store.rs`: версионирование схемы + `migrate()`, бэкап `.bak.<version>` перед миграцией. (← T-012)
- [x] **T-014** `store.rs`: обработка битого JSON — переименование в `.corrupt.<ts>`, старт с дефолтами, возврат предупреждения вызывающему. (← T-012)
  - Приёмка: тест с мусорным файлом; приложение не паникует.
- [x] **T-015** `config.rs`: структура `Config` со всеми полями из PLAN § 4.2, дефолты, валидация диапазонов (`font_size` 10–32, `measure_ch` 40–120). (← T-012)
- [x] **T-016** `projects.rs`: `Project`, `ProjectStore`, CRUD, ULID-идентификаторы, ленивая проверка существования пути с флагом `available`. (← T-012)
  - Приёмка: `projects_remove` не трогает файлы на диске — тест проверяет наличие файлов после удаления записи.
- [x] **T-017** `search.rs`: нечёткий поиск по проектам на `nucleo` (имя + путь), инкрементальное обновление индекса. (← T-016)
  - Приёмка: bench — 10 000 проектов, один запрос < 3 ms.
- [x] **T-018** Property-тесты: round-trip сериализации конфига и проектов (`proptest`). (← T-015, T-016)
- [x] **T-019** ADR-001…ADR-007 записать в `docs/adr/`.

---

## P2 — `ps-core`: файловые операции

- [x] **T-020** 🔒 `fsops::resolve(root, rel)` — единственная точка входа в ФС. Реализовать все 5 проверок из PLAN § 7.1 (запрет `..`, canonicalize + `starts_with`, симлинки, NFC-нормализация, длина имени).
  - Приёмка: property-тест `proptest` — для любой входной строки результат либо `Err`, либо путь внутри корня. Минимум 10 000 кейсов.
- [x] **T-021** `fsops`: `mkdir`, `create_file`, `rename` с валидацией имени и разрешением конфликтов (`name 2`). (← T-020)
- [x] **T-022** `fsops`: `copy` (рекурсивный, с прогрессом для > 50 MB) и стратегии конфликта Replace/KeepBoth/Skip. (← T-020)
- [x] **T-023** `fsops`: `move_items` — `rename` на одном volume, copy+delete между volume с откатом при ошибке. (← T-022)
- [x] **T-024** `fsops`: `trash` через крейт `trash` + безвозвратное удаление отдельной функцией. Точка расширения для снимка `pre_trash` (T-200). (← T-020)
- [x] **T-025** `tree.rs`: ленивое чтение одного уровня каталога, natural sort, папки первыми, фильтр скрытых файлов. (← T-020)
  - Приёмка: bench — каталог с 50 000 файлов читается < 120 ms.
- [x] **T-026** `watch.rs`: `notify` + дебаунс 150 ms + коалесценция событий; ре-скан только раскрытых узлов при переполнении. (← T-025)
- [x] **T-027** Интеграционные тесты файловых операций на `tempfile`: конфликты, кириллица в NFD/NFC, эмодзи в именах, вложенность 20 уровней, файл 0 байт, права только на чтение.

---

## P3 — `ps-render`: конвейер Markdown → HTML

- [x] **T-030** `pipeline.rs`: базовый рендер через `pulldown-cmark` с GFM-опциями из PLAN § ADR-002.
- [x] **T-031** `toc.rs`: slug-идентификаторы заголовков (уникальные при повторах), сборка структуры TOC. (← T-030)
- [x] **T-032** `highlight.rs`: `syntect` с `ClassedHTMLGenerator`, фича `regex-fancy` (без `onig`), ленивая инициализация через `OnceLock`. (← T-030)
  - Приёмка: подсветка даёт классы, а не инлайновые стили; вклад в размер бинарника < 3 MB (`cargo-bloat`).
  - Замер: тестовый бинарник рендера вырос с 2,3 до 4,3 MiB; `syntect` занимает 45,2 KiB секции `.text`. После повторного использования одинаковых блоков 1 MiB рендерится за 11,895 ms, первый чанк 5 MiB — за 59,236 ms.
- [x] **T-033** `mermaid.rs`: перехват fenced-блоков `mermaid` → `<figure class="mermaid" data-hash>` с исходником в `<template>`. (← T-030)
- [x] **T-034** `links.rs`: переписывание относительных путей изображений и ссылок в `asset://`, проверка выхода за корень проекта, различение внешних ссылок и ссылок на `.md`. (← T-030, T-020)
- [x] **T-035** `sanitize.rs`: `ammonia` при `allow_raw_html=false`; запрет `javascript:` и `data:` в `href`. (← T-030)
  - Приёмка: тест-корпус XSS-векторов, ни один не проходит.
- [x] **T-036** Разбиение на чанки по границам блоков верхнего уровня (~64 KB), обёртка в `<section class="chunk">` с `content-visibility:auto`. (← T-030)
- [x] **T-036a** `blocks.rs`: переход на `Parser::into_offset_iter`, разбиение на блоки верхнего уровня с байтовыми диапазонами, `data-block="<i>"` и `data-src-line="<n>"` на каждом блоке, blake3 каждого блока. Фундамент для P9. (← T-030)
  - Приёмка: для каждого блока byte-range и номер строки соответствуют исходнику; проверено на корпусе из T-038.
- [x] **T-037** `cache.rs`: LRU в памяти по `blake3(bytes + options)`, лимиты 16 документов / 64 MB; дисковый кэш с потолком 200 MB и очисткой. (← T-036)
- [x] **T-038** Snapshot-тесты (`insta`) на корпусе: CommonMark-подмножество, таблицы, сноски, чек-листы, mermaid, математика, вложенные списки, сырой HTML, битый Markdown, кириллица/эмодзи/RTL. (← T-030…T-036)
- [x] **T-039** Benchmarks (`criterion`): документы 10 KB / 100 KB / 1 MB / 5 MB. Зафиксировать baseline. (← T-036)
  - Приёмка: 1 MB рендерится < 60 ms; первый чанк 5 MB — < 150 ms.
- [x] **T-040** Fuzz-таргет `cargo-fuzz` на `render()`; 30-минутный прогон без крашей. (← T-030)
  - Финальный прогон 2026-08-15 на полном конвейере: 1 800 секунд, 763 224 выполнения, 4 587 новых единиц, ноль крашей.
  - Два найденных до исправления краша `pulldown-cmark` на управляющих символах и одиночном `CR` сохранены как минизированные регрессионные тесты.

---

## P4 — `ps-app`: оболочка Tauri

- [x] **T-045** Каркас приложения: `main.rs`, `AppState`, восстановление геометрии окна (`tauri-plugin-window-state`).
  - Замер: инициализация `AppState` занимает в среднем 0,170 ms на 1 000 изолированных временных корнях; release-бинарник вырос на 166 000 байт.
  - Проверка macOS: окно 920×640 в позиции (240, 180) точно восстановилось и стало видимым после перезапуска unsigned `.app`.
- [ ] **T-046** Прозрачный титлбар + вибрантный сайдбар (`window-vibrancy`, материал `Sidebar`), корректное положение traffic lights. (← T-045)
- [x] **T-047** Генерация TypeScript-типов из Rust (`ts-rs` или `specta`); CI падает, если сгенерированные типы не закоммичены. (← T-045)
- [x] **T-048** Команды: `config_get/set`, `projects_list/add/rename/remove` — тонкие обёртки над `ps-core`. (← T-015, T-016, T-047)
- [x] **T-049** Команды: `tree_read_dir`, `fs_*` — обёртки над `fsops`. (← T-021…T-025)
- [x] **T-050** Команды: `doc_open` (первый чанк синхронно + события `doc://chunk`), `doc_source`. (← T-036)
- [ ] **T-051** Нативное меню macOS + все горячие клавиши из PLAN § 11.3. (← T-045)
- [ ] **T-052** Динамический `asset://` scope, ограниченный корнями зарегистрированных проектов; обновление при изменении списка. (← T-016)
  - Приёмка: попытка загрузить файл вне корня проекта отклоняется.
- [ ] **T-053** CSP из PLAN § 13 в `tauri.conf.json`; внешние ссылки открываются в системном браузере.
- [ ] **T-054** Одна инстанция приложения: advisory-lock, повторный запуск фокусирует существующее окно.
- [ ] **T-055** Логирование в `~/.1537paperstreet/logs/` с ротацией; в логи не попадает содержимое документов.

---

## P5 — UI: каркас и дизайн-система

- [ ] **T-060** Токены дизайн-системы в CSS-переменных: цвета, типографическая шкала, отступы (шаг 4 px), радиусы, длительности анимаций. (← T-002)
- [ ] **T-061** Три-панельный layout, перетаскиваемые разделители, схлопывание панелей (⌘1/⌘2), сохранение ширин в конфиг. (← T-060, T-048)
- [ ] **T-062** Загрузчик тем: 12 встроенных тем из PLAN § 11.2 в `themes/*.json` + пользовательские из `~/.1537paperstreet/themes/`. Невалидная тема пропускается с логом. (← T-060)
- [ ] **T-063** Переключение темы одним атрибутом на `<html>`; следование системному светлому/тёмному режиму. (← T-062)
  - Приёмка: замер в Web Inspector — переключение ≤ 16 ms, без ре-рендера документа.
- [ ] **T-064** `ipc.ts`: типизированная обёртка над `invoke` + `ipc.mock.ts` для тестов. (← T-047)
- [ ] **T-065** Обработка ошибок в UI: неблокирующие тосты, ни одного `alert`, ни одного молчаливого проглатывания ошибки.
- [ ] **T-066** Доступность: видимые фокус-кольца, полная навигация с клавиатуры, ARIA-роли для дерева и списков, `prefers-reduced-motion`.

---

## P6 — Панель проектов

- [ ] **T-070** Виртуализированный список проектов (в DOM только видимые строки + буфер 20). (← T-061)
  - Приёмка: 10 000 проектов, скролл без кадров > 16.7 ms.
- [ ] **T-071** Добавление проекта: нативный выбор папки, имя по умолчанию из имени папки, валидация дубликатов пути. (← T-048)
- [ ] **T-072** Строка поиска с дебаунсом 30 ms, подсветка совпавших символов, навигация стрелками. (← T-017, T-070)
- [ ] **T-073** Контекстное меню проекта: Open · Rename · Reveal in Finder · **Export…** · Remove from list. (← T-070)
  - Приёмка: Remove показывает пояснение «файлы на диске останутся» и не трогает ни диск, ни историю проекта.
- [ ] **T-074** Индикация недоступного проекта (папка удалена/на отключённом диске) + действие «Найти папку заново». (← T-016)
- [ ] **T-075** Палитра быстрого переключения проектов (⌘⇧P). (← T-072)

---

## P7 — Файловое дерево

- [ ] **T-080** Виртуализированное дерево с ленивым раскрытием узлов; сохранение раскрытых узлов между сессиями. (← T-049)
- [ ] **T-081** Иконки типов файлов (набор с открытой лицензией или собственный SVG-набор), отличие Markdown от прочих файлов. (← T-080)
- [ ] **T-082** Контекстное меню: New File · New Folder · Rename · Duplicate · Copy · Move to… · Reveal in Finder · Open in External Editor · Move to Trash. (← T-080, T-049)
- [ ] **T-083** Drag & drop внутри дерева (перемещение, с ⌥ — копирование) + импорт из Finder. Визуальная индикация цели. (← T-082)
- [ ] **T-084** Диалог разрешения конфликтов: Replace / Keep Both / Skip, с чекбоксом «применить ко всем». (← T-022)
- [ ] **T-085** Живое обновление дерева по событиям `fs://changed` без потери выделения и позиции скролла. (← T-026, T-080)
- [ ] **T-086** Множественное выделение (⇧ и ⌘) и групповые операции. (← T-082)
- [ ] **T-087** Быстрое открытие файла (⌘P) — фаззи-поиск по путям внутри проекта. (← T-080)

---

## P8 — Вьюер

- [ ] **T-090** Отрисовка чанков документа с прогрессивной догрузкой по событиям `doc://chunk`. (← T-050)
  - Приёмка: документ 5 MB — первый экран виден < 150 ms.
- [ ] **T-091** Каркас переключения режимов Preview / Editor / Split (⌘E, ⌘⇧E) с сохранением позиции скролла по якорю ближайшего заголовка. В P8 реализуется только Preview; Editor и Split подключаются в P9. (← T-090)
- [ ] **T-092** Режим только чтения: плашка с причиной (не UTF-8, > 8 MB, нет прав, бинарный файл) вместо молчаливого игнорирования ввода. (← T-091)
- [ ] **T-093** Управление размером шрифта: ⌘+ / ⌘− / ⌘0, шаг 1 px, диапазон 10–32, сохранение в конфиг. (← T-015)
- [ ] **T-094** Настройки типографики: выбор основного и моноширинного шрифта (только системные и открытые), межстрочный интервал, ширина колонки. (← T-093)
- [ ] **T-095** Панель оглавления (TOC): подсветка активного раздела при скролле, схлопывание. (← T-031)
- [ ] **T-096** Навигация: ссылки на `.md` внутри проекта открываются во вьюере, внешние — в браузере, якоря скроллят с плавной анимацией. (← T-034)
- [ ] **T-097** Изображения и медиа через `asset://`, ленивая загрузка, `img` с зарезервированными размерами (без сдвигов layout). (← T-052)
- [ ] **T-098** Поиск по документу (⌘F): подсветка, счётчик, переходы ⌘G / ⌘⇧G. (← T-090)
- [ ] **T-099** Экран настроек (⌘,): внешний вид, типографика, файлы, обновления. (← T-015)
- [ ] **T-100** Пустые состояния и состояния ошибок: нет проектов, пустая папка, файл удалён, бинарный файл, файл > 8 MB, файл только для чтения. (← T-090)

---

## P9 — Редактор

> Фаза с самым высоким риском в проекте: здесь появляется путь записи на диск.
> Задачи T-160, T-163, T-164 помечены 🔒 — тесты пишутся до реализации, без исключений.

### Чтение и запись документов (`ps-core::docio`)

- [ ] **T-160** 🔒 `docio::read_doc` — чтение с определением кодировки, BOM, типа переносов строк (LF/CRLF), наличия финального перевода строки, флага `writable`. Не-UTF-8 → `ReadOnly` с причиной. (← T-020)
  - Приёмка: unit-тесты на 8 комбинаций (LF/CRLF × BOM/без × финальный перевод строки/без).
- [ ] **T-161** `docio::write_doc` — восстановление BOM, EOL и финального перевода строки перед записью; атомарная запись temp → fsync → copy permissions → rename. (← T-160)
  - Приёмка: **round-trip побайтово** — открыть и сохранить без правок ⇒ `cmp` файла до и после не находит различий. Все 8 комбинаций из T-160.
- [ ] **T-162** Пропуск записи, если байты буфера идентичны байтам на диске. (← T-161)
  - Приёмка: `mtime` файла не меняется; watcher не поднимает событие.
- [ ] **T-163** 🔒 Обнаружение конфликта: `write_doc` принимает `base_hash`, сверяет с фактическим хешем файла, при расхождении возвращает `Conflict` и **не пишет**. (← T-161)
  - Приёмка: тест — файл изменён сторонним процессом между открытием и сохранением, данные на диске остались чужими.
- [ ] **T-164** 🔒 `drafts.rs`: при неудачной записи буфер сбрасывается в `~/.1537paperstreet/drafts/<blake3(abs_path)>.md` + метаданные; `drafts_list` / `drafts_resolve`. (← T-161)
  - Приёмка: тесты на «нет прав», «диск полон» (через ограниченный tmpfs или мок), «том отключён».
- [ ] **T-165** Тест устойчивости: `kill -9` процесса во время записи файла 1 MB, 100 повторов ⇒ ни одного битого или пустого файла. (← T-161)
- [ ] **T-166** Bench: запись файла 1 MB ≤ 20 ms; сериализация и запись не блокируют главный поток. (← T-161)

### Инкрементальный рендер (`ps-render::incremental`)

- [ ] **T-167** `incremental.rs`: дифф блоков по blake3, генерация `BlockPatch[] { index, op: Replace|Insert|Delete, html }`. (← T-036a)
- [ ] **T-168** 🔒 Property-тест: случайный документ + случайная правка ⇒ результат инкрементального рендера **побайтово равен** полному. Минимум 10 000 кейсов (`proptest`). (← T-167)
- [ ] **T-169** Страховка: сверка хеша всего документа раз в 2 с простоя, при расхождении — полный ре-рендер + запись в лог. (← T-167)
- [ ] **T-170** Bench инкрементального рендера: правка внутри абзаца в документах 100 KB / 1 MB / 5 MB. (← T-167)
  - Приёмка: 100 KB ≤ 10 ms, 1 MB ≤ 25 ms на стороне Rust.
- [ ] **T-171** Fuzz-таргет на `incremental`. (← T-167)

### Команды Tauri

- [ ] **T-172** Команды `doc_save`, `doc_stat`, `render_incremental`, `assets_import`, `drafts_list`, `drafts_resolve`; события `doc://external_change`, `doc://save_failed`. (← T-161, T-167, T-047)
- [ ] **T-173** `assets.rs` + `assets_import`: приём байтов изображения, определение расширения по MIME, сохранение в `<project>/<assets_dir>/` с именем по дате и хешу, возврат относительного пути. Путь проходит через `fsops::resolve`. (← T-020)
- [ ] **T-174** Пометка собственных записей приложения, чтобы watcher не считал автосохранение внешним изменением. (← T-026, T-161)
  - Приёмка: 100 автосохранений подряд не порождают ни одного `doc://external_change`.

### UI редактора

- [ ] **T-175** Интеграция CodeMirror 6: динамический импорт при первом входе в режим Editor, подсветка Markdown, мягкий перенос, подсветка текущей строки, номера строк (опция). (← T-091)
  - Приёмка: документ без редактирования не грузит ни байта CodeMirror; первый вход в режим ≤ 120 ms.
- [ ] **T-176** Тема CodeMirror генерируется из токенов активной темы приложения; переключение темы не пересоздаёт редактор. (← T-062, T-175)
- [ ] **T-177** Единая типографика редактора и превью: та же гарнитура, кегль и ширина колонки; ⌘+/⌘−/⌘0 действуют в обоих режимах. (← T-093, T-175)
- [ ] **T-178** Редакторские удобства: автопродолжение списков, чек-боксов и цитат по Enter; Tab / ⇧Tab меняют уровень элемента списка; автопарные скобки и кавычки. (← T-175)
- [ ] **T-179** Форматирование выделения: ⌘B, ⌘I, ⌘K, ⌘⇧K, ⌘⇧1…6, ⌘⇧L, ⌘⇧. — корректная работа с пустым выделением и с уже применённым форматированием (toggle off). (← T-175)
- [ ] **T-180** Вставка изображения из буфера обмена → `assets_import` → относительная ссылка в тексте. Перетаскивание файла из дерева и из Finder в редактор → относительная ссылка. (← T-173, T-175)
- [ ] **T-181** Автосохранение в UI: 800 ms простоя · потеря фокуса окна · смена файла · закрытие окна · ⌘S. Индикатор несохранённых изменений в дереве и заголовке. (← T-172, T-175)
  - Приёмка: закрытие окна с несохранёнными изменениями сохраняет их, не показывая диалога.
- [ ] **T-182** Разрешение конфликта: баннер «Оставить мою версию / Загрузить с диска / Показать различия» + простой построчный diff-просмотр. Чистый буфер перезагружается тихо с сохранением курсора и скролла. (← T-163, T-172)
- [ ] **T-183** Баннер ошибки сохранения с кнопкой «Сохранить как…»; диалог восстановления черновиков при старте. (← T-164)
- [ ] **T-184** Режим Split: живое превью с дебаунсом 120 ms, патч DOM по `data-block`, не чаще раза за кадр. (← T-167, T-175)
  - Приёмка: ввод символа → обновление превью ≤ 30 ms (100 KB) и ≤ 60 ms (1 MB); при документе > 2 MB Split деградирует до ручного обновления с явной кнопкой.
- [ ] **T-185** Синхронный скролл по `data-src-line`, флаг ведущей панели с таймаутом 100 ms (защита от петли), отключаемо в настройках. (← T-184)
- [ ] **T-186** Найти и заменить в редакторе (⌘F / ⌘⌥F), опционально regex. (← T-175)
- [ ] **T-187** ⌘N создаёт файл в текущей папке дерева и сразу открывает в редакторе с инлайн-редактированием имени; опция `rename_file_from_h1` (по умолчанию выключена). (← T-082, T-175)
- [ ] **T-188** Раздел «Editor» в настройках: все поля `config.editor` из PLAN § 4.2. (← T-099)
- [ ] **T-189** Ручная проверка IME: ввод на японском, китайском и корейском, системная диктовка, проверка орфографии. Результаты — в `docs/release-checklist.md`. (← T-175)

---

## P10 — История и откат

> Зависит от `docio` (P9): снимок хранит те же байты, что уходят на диск.
> T-190 и T-197 помечены 🔒 — это последний рубеж защиты пользовательского текста.

### Хранилище (`ps-core::history`)

- [ ] **T-190** 🔒 `history/objects.rs`: глобальный content-addressed store `~/.1537paperstreet/history/objects/<ab>/<blake3>.zst`, атомарная запись, проверка blake3 при чтении. (← T-011)
  - Приёмка: подменённый байт в объекте обнаруживается при чтении; снимок помечается утраченным, приложение продолжает работать.
- [ ] **T-191** `history/index.rs`: SQLite (WAL) на проект, имя файла — `blake3(canonical_path)`. Таблица снимков: id, ts, rel_path, blake3, size, reason, pinned, label. (← T-190)
  - Приёмка: удаление проекта из списка и повторное добавление той же папки ⇒ история подхватывается.
- [ ] **T-192** `history::snapshot(project, rel_path, bytes, reason)` — пропуск, если хеш совпадает с последним снимком этого файла; работа вне главного потока. (← T-191)
- [ ] **T-193** `history/policy.rs`: прореживание по ярусам (всё за 24 ч · почасовые за 7 дней · дневные за 90 дней · закреплённые навсегда) + лимиты 200 MB на проект и 2 GB глобально. (← T-191)
- [ ] **T-194** 🔒 `history/gc.rs`: mark-and-sweep без счётчиков ссылок, обход всех индексов, запуск в фоне при простое не чаще раза в час. (← T-193)
  - Приёмка: тест на 10 000 объектов — ни один достижимый объект не удалён; недостижимые удалены все.
- [ ] **T-195** Переименование индекса при переносе проекта («Найти папку заново», T-074). (← T-191)
- [ ] **T-196** `history_forget_project` + `history_stats` (объём по проектам и глобально). (← T-194)
- [ ] **T-197** 🔒 `history::restore(snapshot_id, mode)`: перед восстановлением делается снимок `pre_restore`; запись через `docio::write_doc`. Режим `CopyAside` создаёт `имя (2026-08-12 14-03).md`. (← T-161, T-192)
- [ ] **T-198** Восстановление удалённых файлов: путь есть в индексе, файла на диске нет. (← T-197)
- [ ] **T-199** `history_export_git`: одноразовый экспорт истории проекта в обычный git-репозиторий, коммит на снимок. (← T-191)

### Снимки перед разрушительными операциями

- [ ] **T-200** 🔒 Хуки `pre_*` во всех девяти точках из PLAN § 10.3: `pre_trash`, `pre_conflict_discard`, `pre_conflict_overwrite`, `pre_draft_restore`, `pre_restore`, `pre_replace` + границы `close`, `interval`, `manual`. (← T-192, T-024, T-182)
  - Приёмка: отдельный тест на **каждую** из девяти причин — после операции снимок существует и содержит прежние байты. Ни одной точки без теста.
- [ ] **T-201** Периодический снимок: 5 минут непрерывного редактирования, закрытие файла, ⌘S. (← T-181, T-192)

### Трёхсторонний конфликт

- [ ] **T-202** Диалог конфликта использует снимок на `base_hash` как общего предка: раздельно показывает «твои изменения» и «изменения на диске». Выбор по-прежнему одной стороны, merge — v1.1. (← T-182, T-192)

### UI

- [ ] **T-203** Панель History для файла: таймлайн снимков (время, размер, причина), превью содержимого, дифф с текущей версией. Переиспользует diff-компонент из T-182. (← T-191, T-182)
  - Приёмка: 500 снимков — панель открывается ≤ 50 ms.
- [ ] **T-204** Действия в панели: «Восстановить», «Скопировать в новый файл», «Закрепить» с меткой. (← T-197, T-203)
- [ ] **T-205** История проекта: общий таймлайн + отдельный раздел удалённых файлов с восстановлением. (← T-198, T-203)
- [ ] **T-206** Раздел «History» в настройках: включение, интервал, лимиты, занятый объём по проектам, «Забыть историю проекта». (← T-196, T-099)

### Производительность и тесты

- [ ] **T-207** Bench: снимок файла 1 MB ≤ 15 ms; GC хранилища на 2 GB ≤ 3 s и не блокирует UI. (← T-192, T-194)
- [ ] **T-208** Тесты: превышение лимита проекта и глобального, прореживание по ярусам, конкурентная запись, битый индекс SQLite, битый объект, дедупликация одного файла между тремя проектами. (← T-193)

---

## P11 — Mermaid, код, математика

- [ ] **T-105** Вендоринг `mermaid.esm.min.mjs` в репозиторий, фиксация версии и лицензии в `docs/credits.md`.
- [ ] **T-106** Динамический импорт Mermaid только при наличии диаграмм в документе. (← T-105, T-033)
  - Приёмка: документ без диаграмм не грузит ни байта Mermaid (проверка в Network-панели).
- [ ] **T-107** Ленивый рендер по `IntersectionObserver` (rootMargin 400 px), `securityLevel:'strict'`, `htmlLabels:false`. (← T-106)
- [ ] **T-108** Кэш SVG по `blake3(исходник + тема)` в памяти и на диске. (← T-107)
- [ ] **T-109** Синхронизация темы Mermaid с темой приложения через `themeVariables`. (← T-063, T-107)
- [ ] **T-110** Обработка ошибок синтаксиса диаграммы: блок с сообщением и исходником, страница не ломается. (← T-107)
- [ ] **T-111** Модальное окно диаграммы: зум, панорамирование, копирование SVG, сохранение PNG. (← T-107)
- [ ] **T-112** CSS-стили подсветки кода для всех тем + кнопка «Копировать» на блоках кода. (← T-032, T-062)
- [ ] **T-113** Математика (KaTeX), вендоринг, ленивая загрузка по тому же принципу, что и Mermaid. (← T-106)

---

## P12 — Экспорт в ZIP

- [ ] **T-120** `export.rs`: обход проекта с учётом `export_ignore` и `.psignore` (крейт `ignore`), симлинки не разыменовываются. Перед началом экспорта все несохранённые буферы принудительно сохраняются. (← T-020, T-181)
- [ ] **T-121** Потоковая запись ZIP в файл (не в память), запись во временный `.zip.part` с переименованием по завершении. (← T-120)
  - Приёмка: экспорт проекта на 2 GB — пик RSS процесса не растёт более чем на 30 MB.
- [ ] **T-122** Прогресс и отмена: события не чаще 20/с, `CancellationToken`, удаление `.part` при отмене. (← T-121)
- [ ] **T-123** Slug-генерация имени архива, разрешение коллизий (`-1`, `-2`). (← T-121)
- [ ] **T-124** UI: пункт Export в контекстном меню и ⌘⌥E (⌘⇧E занято режимом Split), нативный диалог сохранения, индикатор прогресса, кнопка «Показать в Finder» по завершении. (← T-073, T-122)
- [ ] **T-125** Настройка «не спрашивать путь» — класть архив рядом с папкой проекта. История в архив по умолчанию не входит; отдельная опция «включить историю». (← T-124, T-191)
- [ ] **T-126** Тесты: пустой проект, проект с 100 000 файлов, имена с кириллицей и эмодзи, отмена на середине, нет прав на запись в целевую папку.

---

## P13 — Производительность

- [ ] **T-130** Измерить все бюджеты из PLAN § 1.2, записать фактические значения в `docs/perf.md`.
- [ ] **T-131** Профилирование холодного старта (Instruments Time Profiler); устранить всё, что не нужно до первого кадра.
  - Приёмка: холодный старт ≤ 400 ms на M1.
- [ ] **T-132** Профилирование памяти (Allocations, Leaks); утечек нет, RSS в бюджете.
- [ ] **T-133** Профилирование скролла (Safari Web Inspector) на документе 5 MB; ни одного кадра > 16.7 ms.
- [ ] **T-134** `cargo-bloat`: разобрать топ-20 по размеру, урезать лишнее. Итог — DMG ≤ 20 MB.
- [ ] **T-135** CI-джоба регресса производительности: `criterion` + `hyperfine` против `benches/baseline.json`, падение > 10 % ⇒ красный CI. (← T-039)
- [ ] **T-136** Прогон на «тяжёлых» данных: 10 000 проектов, проект с 200 000 файлов, документ 5 MB с 50 диаграммами, час непрерывной печати в режиме Split (проверка на утечки и деградацию), хранилище истории на 2 GB с 50 000 снимков. Зафиксировать результаты.

---

## P14 — Тесты и документация

- [ ] **T-140** Довести покрытие: `ps-core` и `ps-render` ≥ 85 %, `ps-app` ≥ 60 %, логика UI ≥ 70 %. Модули `docio`, `incremental` и `history` — ≥ 95 %, без исключений. Включить порог как блокирующий в CI.
- [ ] **T-141** Playwright-сценарии против мок-IPC: добавление проекта, поиск, создание папки, перемещение файла, экспорт, переключение темы, три режима отображения, изменение шрифта, ввод текста с автосохранением, форматирующие сочетания клавиш, вставка изображения, баннер конфликта, восстановление черновика, откат из панели истории. (← T-064, T-182, T-203)
- [ ] **T-142** Ночной fuzz-прогон в CI, корпус регрессий в репозитории. (← T-040)
- [ ] **T-143** `#![warn(missing_docs)]` без единого предупреждения; doctest'ы для публичного API; `cargo doc` собирается без warning'ов.
- [ ] **T-144** Пользовательская документация (mdBook): все разделы из PLAN § 15, со скриншотами. Разделы «Редактирование и автосохранение» и «Что происходит при конфликте версий» — обязательны и написаны до релиза.
- [ ] **T-145** `docs/release-checklist.md` — ручной smoke-тест перед релизом (~20 пунктов, которые нельзя автоматизировать).
- [ ] **T-146** `docs/credits.md`: лицензии тем, шрифтов, Mermaid, KaTeX, иконок.
- [ ] **T-147** `README.md` с быстрым стартом (< 5 минут до первого запуска) и `CONTRIBUTING.md`.

---

## P15 — Упаковка и релиз

- [ ] **T-150** Universal binary (`aarch64` + `x86_64`, `lipo`), минимальная версия macOS 12.0.
- [ ] **T-151** Иконка приложения: `.icns` во всех размерах, включая @2x.
- [ ] **T-152** Подпись Developer ID + hardened runtime + минимальные entitlements. (← T-008)
- [ ] **T-153** Нотаризация (`notarytool` + `stapler`) в CI по тегу. (← T-152)
- [ ] **T-154** DMG с фоном и симлинком на `/Applications`. (← T-153)
- [ ] **T-155** Авто-обновления (`tauri-plugin-updater`, подпись Ed25519), выключаемо в настройках.
- [ ] **T-156** Homebrew cask.
- [ ] **T-157** Релизный workflow: тег → сборка → подпись → нотаризация → GitHub Release с `CHANGELOG`.
- [ ] **T-158** Установка и проверка на чистой машине без установленных dev-инструментов. Пройти `docs/release-checklist.md`. (← T-145)

---

## Бэклог после v1

- [ ] WYSIWYG-режим редактирования
- [ ] История версий и снапшоты документов
- [ ] Git-бэкенд истории: снимки в приватный ref `refs/1537paperstreet`
- [ ] Read-only интеграция с git: бейджи статуса в дереве, история файла из коммитов, явное «Закоммитить изменения…»
- [ ] Полноценное трёхстороннее слияние при конфликте версий
- [ ] Vim/Emacs-режимы, сниппеты и шаблоны документов
- [ ] Полнотекстовый поиск по содержимому всех файлов проекта
- [ ] Экспорт документа в PDF и standalone HTML
- [ ] Wiki-ссылки `[[...]]` и backlinks
- [ ] Вкладки и несколько окон
- [ ] Граф связей между документами
- [ ] Windows и Linux
