//! Markdown rendering for 1537paperstreet.

#![warn(missing_docs)]

/// Intentional Clippy failure used to verify the pull-request quality gate.
pub fn clippy_gate_probe(value: bool) -> bool {
    if value { true } else { false }
}
